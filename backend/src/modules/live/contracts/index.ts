/**
 * Live's public surface.
 *
 * Other modules react to live events rather than calling in; this exports only
 * the vocabulary needed to interpret them.
 */
export type { ParticipantRole } from './participant-role';
